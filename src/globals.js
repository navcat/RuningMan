var g_groundHeight = 57;
var g_runnerStartX = 80;

if(typeof TagOfLayer == "undefined"){
	var TagOfLayer = {};
	TagOfLayer.BackGround = 0;    	// 背景层
	TagOfLayer.Animation = 1;		// 动画层
	TagOfLayer.Status = 2;			// 状态层
};